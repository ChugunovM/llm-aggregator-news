# 🗞️ LLM News Aggregator (POC)

> **Локальный агрегатор новостей с LLM-анализом**  
> Собирает новости о компаниях из RSS, официальных сайтов и Telegram → суммаризирует через Ollama → отправляет уведомления


## ✨ Возможности

- **Автоматический поиск источников** по названию компании
- **Мониторинг RSS, официальных сайтов и Telegram-каналов**
- **LLM-анализ** (суммаризация, классификация, тональность) через **локальную модель Ollama**
- **Подписки с автообновлением** (каждые 2/6/24 часа)
- **Пуш-уведомления в Telegram**
- **Полностью локальный запуск** — никаких внешних API

## 🛠️ Требования

- Docker
- Ollama (локально)
- Python 3.11+

## 🚀 Установка

1. Клонируй репозиторий:
   ```bash
   git clone https://github.com/ChugunovM/llm-news-aggregator.git
   cd llm-aggregator-news
   ```

2. Установи зависимости:
   ```bash
   ./setup.sh
   ```

3. Настрой `.env`:
   ```env
   # Telegram API (для парсинга)
   TELEGRAM_API_ID=12345678
   TELEGRAM_API_HASH=abcdef...

   # Telegram Bot (для уведомлений)
   TELEGRAM_BOT_TOKEN=123456789:AAFd...
   TELEGRAM_CHAT_ID=123456789

   # Ollama
   OLLAMA_HOST=http://host.docker.internal:11434
   ```

4. Запусти:
   ```bash
   docker-compose up -d
   ```

## 🌐 Использование

- **UI**: http://localhost:8502
- **API**: http://localhost:8000/docs

1. Введи название компании (например, `Apple`)
2. Нажми «Собрать и подписаться»
3. Получай уведомления в Telegram

## 🧠 Архитектура

```
[Streamlit UI] 
    ↔ [FastAPI] 
        → [Celery Beat] → [Celery Worker] 
            → [RSS/HTML/Telegram Adapters] 
                → [Ollama LLM] 
                    → [PostgreSQL]
                        → [Telegram Notifications]
```

## 🔒 Приватность

- Все данные обрабатываются **локально**
- Ollama работает на твоей машине
- Никакие данные не уходят в облако

## 📦 Технологии

- **Backend**: FastAPI, Celery, SQLAlchemy
- **LLM**: Ollama (Mistral 7B)
- **Frontend**: Streamlit
- **Инфраструктура**: Docker, PostgreSQL, Redis

## 📝 Лицензия

MIT
```

---
